{
  "name": "91radio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LAN radio: one live stream, shared queue",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
